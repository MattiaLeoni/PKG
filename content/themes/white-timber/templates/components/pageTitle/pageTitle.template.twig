<div class="article-image" uk-parallax="bgy: -200" style="background-image:url({{img_url}})">
    <div class="container mx-auto px-4 uk-height-1-1 uk-position-relative pt-20 pb-10 xl:pt-32">
        {% include 'common/breadcrumbs.twig' %}
        <h1 class="text-white z-10 relative font-semibold">{{ title }}</h1>
        {% if subtitle %}
            <h4 class="mt-1 text-white text-xl z-10 relative font-semibold">{{subtitle}}</h4>
        {% endif %}
        
    </div>
</div>
